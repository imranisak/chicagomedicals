actor: AcceptanceTester
modules:
  enabled:
    - PhpBrowser:
        url: http://chichagomedicals/
    - \Helper\Acceptance
    - Db:
        dsn: 'mysql:host=localhost;dbname=chmeds_testing'
        user: 'root'
        password: ''
        cleanup: true # run populator before each test
        populate: true # run populator before all test
